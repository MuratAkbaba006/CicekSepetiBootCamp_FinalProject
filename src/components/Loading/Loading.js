import React from 'react';
import ReactLoading from 'react-loading';

const Loading = () => {
  return (
    <ReactLoading type="bubbles" color="#4B9CE2" height={100} width={500} />
  );
};

export default Loading;
